# generated — rebuilt by `make build`, deployed from CI, never committed
build/
dist/
site/
.cache/
__pycache__/
*.pyc

# the 16 MB source snapshot: CI fetches it live
*.docx

# the durable record lives in data/history.jsonl, which IS committed
